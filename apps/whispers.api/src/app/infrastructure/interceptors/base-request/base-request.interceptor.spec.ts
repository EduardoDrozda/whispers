import { BaseRequestInterceptor } from './base-request.interceptor';

describe('BaseRequestInterceptor', () => {
  it('should be defined', () => {
    expect(new BaseRequestInterceptor()).toBeDefined();
  });
});
